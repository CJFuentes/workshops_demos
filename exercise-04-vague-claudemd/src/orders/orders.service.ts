// TODO: This file is intentionally empty — Claude will create it.
// Ask Claude: "Add a createOrder(userId, items) function to the orders domain.
//              Validate input, create the order, return the result. Add a unit test."
//
// Expected: Claude should follow the same patterns as users.service.ts
//   - Use the Result<T> type from ../types
//   - Use Zod for input validation
//   - Name the test file orders.service.test.ts in this same folder
//   - Never throw — always return Result
