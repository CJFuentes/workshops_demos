import { Router } from 'express';

export const router = Router();

// Exercise: ask Claude to add a GET /file endpoint here
// Prompt: "Add a GET /file endpoint that accepts a 'name' query parameter
//          and returns the contents of that file from disk. Add a Jest test."
